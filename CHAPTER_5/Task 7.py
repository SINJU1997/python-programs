#Write a Python program to configure the rounding to round to the floor, ceiling

import math

num=30.86
rfloor=math.floor(num)
rceil=math.ceil(num)
print("Number is %f, and it's Rounded value is %d" %(num,rfloor))
print("Number is %f, and it's Rounded value is %d" %(num,rceil))
